# Теоретическая база для лабораторной работы

## 🎯 Основные понятия цифрового двойника ОЗУ

**Цифровой двойник** — это виртуальная модель физического объекта, которая:

* Точно имитирует поведение реального ОЗУ
* Позволяет моделировать неисправности без риска для оборудования
* Обеспечивает тестирование алгоритмов обнаружения ошибок

---

## 🔧 Моделируемые неисправности ОЗУ

### 1. **Stuck-at-0 (Залипание в 0)**

* **Суть**: Все биты ячейки всегда читаются как `0`
* **Физическая причина**: Обрыв цепи, короткое замыкание на землю
* **Математическая модель**: `read(addr) = 0x00000000`

### 2. **Stuck-at-1 (Залипание в 1)**

* **Суть**: Все биты ячейки всегда читаются как `1`
* **Физическая причина**: Короткое замыкание на питание
* **Математическая модель**: `read(addr) = 0xFFFFFFFF`

### 3. **Bit-flip (Случайная инверсия)**

* **Суть**: Каждый бит инвертируется с заданной вероятностью
* **Физическая причина**: Радиационные воздействия, электромагнитные помехи
* **Математическая модель**: `read(addr) = write(addr) ⊕ random_mask`

### 4. **Open Read (Обрыв чтения)**

* **Суть**: Чтение всегда возвращает фиксированное значение
* **Физическая причина**: Обрыв шины данных, неисправность усилителей
* **Математическая модель**: `read(addr) = 0xFFFFFFFF` (при нормальной записи)

---

## 🧪 Алгоритмы обнаружения неисправностей

### Принцип обнаружения: **Контроль по четности**

Все алгоритмы основаны на записи известного паттерна и проверке его сохранности:

```text
Запись(expected) → Чтение(actual) → Сравнение(expected == actual)
```

### 1. **Walking Ones (Шагающие единицы)**

**Алгоритм:**

```cpp
for each address a:
    pattern = 1 << (a % 32)  // Одна единица в позиции (a mod 32)
    write(a, pattern)

for each address a:
    expected = 1 << (a % 32)
    actual = read(a)
    check(expected == actual)
```

**Обнаруживаемые неисправности:**

* **Stuck-at-0**: Единица не может быть записана
* **Stuck-at-1**: Появляются лишние единицы
* **Bit-flip**: Изменяется количество единиц
* **Короткие замыкания**: Единица "протекает" в соседние биты

### 2. **Walking Zeros (Шагающие нули)**

**Алгоритм:**

```cpp
for each address a:
    pattern = ~(1 << (a % 32))  // Один ноль в позиции (a mod 32)
    write(a, pattern)

for each address a:
    expected = ~(1 << (a % 32))
    actual = read(a)
    check(expected == actual)
```

**Обнаруживаемые неисправности:**

* **Stuck-at-1**: Ноль не может быть записан
* **Stuck-at-0**: Появляются лишние нули
* **Bit-flip**: Изменяется количество нулей
* **Обрывы**: Ноль "исчезает"

### 3. **March Simple (Простой маршевый тест)**

**Алгоритм:**

```cpp
// Фаза 1: Все нули
for each address a: write(a, 0x00000000)
for each address a: check(read(a) == 0x00000000)

// Фаза 2: Все единицы  
for each address a: write(a, 0xFFFFFFFF)
for each address a: check(read(a) == 0xFFFFFFFF)
```

**Обнаруживаемые неисправности:**

* **Stuck-at-0/1**: Невозможность изменить состояние
* **Bit-flip**: Несоответствие полного паттерна
* **Нестабильность**: Изменение состояния между записью и чтением

---

## 🎲 Теория комбинаторного покрытия

### Почему это работает при ЛЮБЫХ комбинациях неисправностей?

#### 1. **Принцип ортогональности тестов**

Каждый алгоритм проверяет **ортогональные свойства** памяти:

| Алгоритм      |        Проверяемое свойство | Чувствительность              |
| ------------- | --------------------------: | ----------------------------- |
| Walking Ones  | Способность хранить единицы | Stuck-at-0, битовые залипания |
| Walking Zeros |    Способность хранить нули | Stuck-at-1, битовые обрывы    |
| March Simple  |     Целостность полных слов | Глобальные неисправности      |

#### 2. **Полнота битового покрытия**

**Walking Ones/Zeros** обеспечивают:

* **100% покрытие битов**: Каждый бит проверяется индивидуально
* **Все возможные позиции**: От младшего (bit 0) до старшего (bit 31)
* **Изоляция битов**: Позволяют определить ТОЧНЫЙ неисправный бит

#### 3. **Комплементарность проверок**

```text
Walking Ones:    проверяет 0→1 переходы
Walking Zeros:   проверяет 1→0 переходы  
March Simple:    проверяет массовые 0→1 и 1→0 переходы
```

### Математическое обоснование

#### Теорема: **Любая детерминированная неисправность будет обнаружена**

**Доказательство (схематично):**

1. Любая неисправность проявляется как:

   * Неправильное значение при чтении, или
   * Невозможность записи определенного значения
2. Множество тестовых паттернов покрывает ВСЕ возможные состояния:

   * `{000...0}` (March)
   * `{111...1}` (March)
   * `{000...1}` (Walking Ones)
   * `{111...0}` (Walking Zeros)
   * И все промежуточные состояния
3. **Полный набор состояний** = гарантия обнаружения

#### Для вероятностных неисправностей (Bit-flip)

**Статистическое обнаружение:**

* Многократное тестирование увеличивает вероятность обнаружения
* Вероятность пропуска ошибки: `P_miss = (1 - p_flip)^n`
* При `n → ∞`, `P_miss → 0`

---

## 📊 Теория эффективности тестирования

### Метрики покрытия

1. **Адресное покрытие**: % протестированных адресов

```text
Coverage_addr = (tested_addresses / total_addresses) × 100%
```

2. **Битовое покрытие**: % протестированных битовых комбинаций

```text
Coverage_bit = (unique_bit_patterns / 2^32) × 100%
```

3. **Неисправностное покрытие**: % обнаруженных внедренных неисправностей

```text
Coverage_fault = (detected_faults / injected_faults) × 100%
```

**Золотое правило тестирования памяти:**

> Для гарантированного обнаружения любой постоянной неисправности достаточно проверить все возможные переходы 0→1 и 1→0 для каждого бита.

---

## 🔬 Практические аспекты в вашей реализации

### 1. **Моделирование неисправностей**

```cpp
// В read() - применение неисправности при чтении
switch(_injected.model) {
    case FaultModel::StuckAt0: return 0u;
    case FaultModel::StuckAt1: return static_cast<Word>(~0u);
    // ...
}
```

### 2. **Обнаружение в реальном времени**

```cpp
// Для каждого адреса при тестировании
Word expected = calculateExpectedValue(algo, addr);
Word actual = _mem->read(addr);
bool passed = (expected == actual);
```

### 3. **Визуализация результатов**

* **Красный**: Обнаруженная неисправность
* **Желтый**: Область с внедренной неисправностью
* **Зеленый**: Исправные ячейки

---

## 💡 Ключевые выводы

1. **Комбинация алгоритмов** обеспечивает полное покрытие всех типов неисправностей.
2. **Ортогональность проверок** гарантирует, что неисправность, не найденная одним алгоритмом, будет найдена другим.
3. **Статистические методы** эффективны против вероятностных неисправностей.
4. **Визуализация процесса** позволяет понять механизмы обнаружения ошибок.

---

*Эта теоретическая база объясняет, почему реализация способна обнаруживать ЛЮБУЮ комбинацию смоделированных неисправностей при использовании предложенного набора алгоритмов тестирования.*
