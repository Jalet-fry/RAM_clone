# 🖥️ Цифровой двойник ОЗУ — Тестбенч

---

## 📋 Титульный слайд

### **Цифровой двойник ОЗУ — Тестбенч**

*Виртуальная модель оперативной памяти для моделирования неисправностей и тестирования алгоритмов обнаружения ошибок*

---

### 👥 Команда разработки

| Участник | Роль | Компонент |
|----------|------|-----------|
| **Волосевич Витовт Александрович** | Разработка UI | MainWindow (пользовательский интерфейс) |
| **Прокопович Даниил Васильевич** | Разработка модели | MemoryModel (модель памяти и неисправности) |
| **Шкелёнок Анастасия Владимировна** | Тестирование | TesterWorker (управление потоками, тестирование) |
| **Харькова Мария Сергеевна** | Алгоритмы | MemoryTester (алгоритмы тестирования) |

### 👨‍🏫 Преподаватели

- **Савчиц Андрей Геннадьевич** — преподаватель
- **Татур Михаил Михайлович** — лектор

---

## 🎯 Цели и задачи проекта

### Основная цель

Создание **цифрового двойника оперативной памяти** для безопасного моделирования неисправностей и тестирования алгоритмов их обнаружения без риска для реального оборудования.

### Задачи проекта

1. ✅ **Моделирование памяти**
   - Реализация виртуальной модели ОЗУ с поддержкой чтения/записи
   - Имитация поведения реальной памяти

2. ✅ **Моделирование неисправностей**
   - Реализация 4 типов неисправностей (Stuck-at-0, Stuck-at-1, Bit-flip, Open Read)
   - Настраиваемые параметры внедрения неисправностей

3. ✅ **Алгоритмы тестирования**
   - Реализация 3 алгоритмов обнаружения неисправностей
   - Walking Ones, Walking Zeros, March Simple

4. ✅ **Пользовательский интерфейс**
   - Интуитивный GUI для управления неисправностями
   - Визуализация результатов тестирования
   - Статистика и метрики покрытия

5. ✅ **Асинхронное тестирование**
   - Многопоточная архитектура
   - Отображение прогресса в реальном времени

---

## 📖 Описание проекта

### Что такое цифровой двойник?

**Цифровой двойник** — это виртуальная модель физического объекта, которая:

- 🎯 **Точно имитирует поведение** реального ОЗУ
- 🛡️ **Позволяет моделировать неисправности** без риска для оборудования
- 🔍 **Обеспечивает тестирование** алгоритмов обнаружения ошибок
- 📊 **Предоставляет метрики** эффективности тестирования

### Зачем это нужно?

| Проблема | Решение |
|----------|---------|
| Тестирование на реальном оборудовании дорого и рискованно | Виртуальная модель позволяет безопасно экспериментировать |
| Сложно воспроизвести специфические неисправности | Моделирование с настраиваемыми параметрами |
| Нужно проверить эффективность алгоритмов | Автоматизированное тестирование с метриками |

### Концепция работы

```
┌─────────────────┐
│  Пользователь   │
│   (Интерфейс)   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐     ┌──────────────────┐
│  Внедрение      │────▶│  Модель памяти   │
│  неисправности  │     │  (MemoryModel)   │
└─────────────────┘     └────────┬─────────┘
                                  │
                                  ▼
┌─────────────────┐     ┌──────────────────┐
│  Запуск теста   │────▶│  Тестирование    │
│  (Алгоритм)     │     │  (MemoryTester)  │
└─────────────────┘     └────────┬─────────┘
                                  │
                                  ▼
┌─────────────────┐     ┌──────────────────┐
│  Результаты    │◀────│  Обнаружение     │
│  и статистика  │     │  неисправностей  │
└─────────────────┘     └──────────────────┘
```

---

## 🏗️ Архитектура системы

### Компоненты системы

#### 1. **MemoryModel** — Модель памяти
- **Ответственность**: Хранение данных, применение неисправностей при чтении
- **Особенности**:
  - Потокобезопасность (QMutex)
  - Размер: 256 слов (32-битных)
  - Неисправности применяются только при чтении

#### 2. **MemoryTester** — Тестирование
- **Ответственность**: Реализация алгоритмов тестирования
- **Алгоритмы**:
  - Walking Ones
  - Walking Zeros
  - March Simple

#### 3. **TesterWorker** — Управление потоками
- **Ответственность**: Асинхронное выполнение тестов
- **Особенности**:
  - Отдельный поток для тестирования
  - Сигналы прогресса и результатов

#### 4. **MainWindow** — Пользовательский интерфейс
- **Ответственность**: UI, визуализация, управление
- **Компоненты**:
  - Панель управления неисправностями
  - Панель тестирования
  - Таблица памяти
  - Журнал событий
  - Статистика

### Взаимодействие компонентов

```
MainWindow
    │
    ├──▶ MemoryModel (чтение/запись, неисправности)
    │
    ├──▶ TesterWorker (асинхронное тестирование)
    │       │
    │       └──▶ MemoryTester (алгоритмы)
    │               │
    │               └──▶ MemoryModel (чтение для проверки)
    │
    └──▶ Отображение результатов
```

### Поток данных

1. **Внедрение неисправности**: `MainWindow` → `MemoryModel.injectFault()`
2. **Запуск теста**: `MainWindow` → `TesterWorker.run()` → `MemoryTester.runTest()`
3. **Применение неисправности**: `MemoryTester` → `MemoryModel.read()` → применяется неисправность
4. **Результаты**: `MemoryTester` → сигнал `finished()` → `MainWindow.onTestFinished()`

---

## 🔧 Моделируемые неисправности ОЗУ

### Общий принцип

**Неисправности применяются только при чтении**, запись всегда сохраняет точное значение. Это имитирует реальное поведение, где неисправность влияет на чтение данных.

### 1. **Stuck-at-0 (Залипание в 0)** 🔴

| Характеристика | Описание |
|----------------|----------|
| **Суть** | Все биты ячейки всегда читаются как `0` |
| **Физическая причина** | Обрыв цепи, короткое замыкание на землю |
| **Математическая модель** | `read(addr) = 0x00000000` |
| **Обнаружение** | Невозможность записать единицу |

**Пример:**
```
Запись: 0xFFFFFFFF
Чтение: 0x00000000  ← всегда 0
```

### 2. **Stuck-at-1 (Залипание в 1)** 🔴

| Характеристика | Описание |
|----------------|----------|
| **Суть** | Все биты ячейки всегда читаются как `1` |
| **Физическая причина** | Короткое замыкание на питание |
| **Математическая модель** | `read(addr) = 0xFFFFFFFF` |
| **Обнаружение** | Невозможность записать ноль |

**Пример:**
```
Запись: 0x00000000
Чтение: 0xFFFFFFFF  ← всегда 1
```

### 3. **Bit-flip (Случайная инверсия)** 🎲

| Характеристика | Описание |
|----------------|----------|
| **Суть** | Каждый бит инвертируется с заданной вероятностью |
| **Физическая причина** | Радиационные воздействия, электромагнитные помехи |
| **Математическая модель** | `read(addr) = write(addr) ⊕ random_mask` |
| **Настройка** | Вероятность инверсии (0.0 - 1.0) |

**Пример:**
```
Запись: 0x00000001
Чтение: 0x00000003  ← бит 1 инвертирован (вероятностно)
```

### 4. **Open Read (Обрыв чтения)** ⚠️

| Характеристика | Описание |
|----------------|----------|
| **Суть** | Чтение всегда возвращает фиксированное значение |
| **Физическая причина** | Обрыв шины данных, неисправность усилителей |
| **Математическая модель** | `read(addr) = 0xFFFFFFFF` |
| **Обнаружение** | Несоответствие записанного и прочитанного |

**Пример:**
```
Запись: 0x12345678
Чтение: 0xFFFFFFFF  ← всегда одно значение
```

---

## 🧪 Алгоритмы обнаружения неисправностей

### Принцип обнаружения

Все алгоритмы основаны на **контроле по четности**:

```
Запись(expected) → Чтение(actual) → Сравнение(expected == actual)
```

### 1. **Walking Ones (Шагающие единицы)** 👣

#### Алгоритм

```cpp
// Фаза 1: Запись эталонных данных
for each address a:
    pattern = 1 << (a % 32)  // Одна единица в позиции (a mod 32)
    write(a, pattern)

// Фаза 2: Чтение и проверка
for each address a:
    expected = 1 << (a % 32)
    actual = read(a)
    check(expected == actual)
```

#### Обнаруживаемые неисправности

- ✅ **Stuck-at-0**: Единица не может быть записана
- ✅ **Stuck-at-1**: Появляются лишние единицы
- ✅ **Bit-flip**: Изменяется количество единиц
- ✅ **Короткие замыкания**: Единица "протекает" в соседние биты

#### Покрытие

- **100% покрытие битов**: Каждый бит проверяется индивидуально
- **Все позиции**: От младшего (bit 0) до старшего (bit 31)

### 2. **Walking Zeros (Шагающие нули)** 👣

#### Алгоритм

```cpp
// Фаза 1: Запись эталонных данных
for each address a:
    pattern = ~(1 << (a % 32))  // Один ноль в позиции (a mod 32)
    write(a, pattern)

// Фаза 2: Чтение и проверка
for each address a:
    expected = ~(1 << (a % 32))
    actual = read(a)
    check(expected == actual)
```

#### Обнаруживаемые неисправности

- ✅ **Stuck-at-1**: Ноль не может быть записан
- ✅ **Stuck-at-0**: Появляются лишние нули
- ✅ **Bit-flip**: Изменяется количество нулей
- ✅ **Обрывы**: Ноль "исчезает"

#### Покрытие

- **Комплементарность**: Проверяет переходы 1→0 (в отличие от Walking Ones 0→1)
- **Полное битовое покрытие**: Все биты проверяются на способность хранить ноль

### 3. **March Simple (Простой маршевый тест)** 🚶

#### Алгоритм

```cpp
// Шаг 1: Запись всех 0
for each address a: write(a, 0x00000000)

// Шаг 2: Чтение всех 0 (ожидаем 0)
for each address a: check(read(a) == 0x00000000)

// Шаг 3: Запись всех 1
for each address a: write(a, 0xFFFFFFFF)

// Шаг 4: Чтение всех 1 (ожидаем 1)
for each address a: check(read(a) == 0xFFFFFFFF)
```

#### Обнаруживаемые неисправности

- ✅ **Stuck-at-0/1**: Невозможность изменить состояние
- ✅ **Bit-flip**: Несоответствие полного паттерна
- ✅ **Нестабильность**: Изменение состояния между записью и чтением

#### Особенности

- **Массовые переходы**: Проверяет все биты одновременно
- **Глобальные неисправности**: Эффективен для обнаружения системных проблем

### Сравнение алгоритмов

| Алгоритм | Проверяемое свойство | Чувствительность | Покрытие |
|----------|---------------------|------------------|----------|
| **Walking Ones** | Способность хранить единицы | Stuck-at-0, битовые залипания | 100% битов, переходы 0→1 |
| **Walking Zeros** | Способность хранить нули | Stuck-at-1, битовые обрывы | 100% битов, переходы 1→0 |
| **March Simple** | Целостность полных слов | Глобальные неисправности | Массовые переходы 0↔1 |

### Теоретическое обоснование

**Теорема**: Любая детерминированная неисправность будет обнаружена комбинацией этих алгоритмов.

**Доказательство**:
1. Любая неисправность проявляется как неправильное значение при чтении или невозможность записи
2. Множество тестовых паттернов покрывает ВСЕ возможные состояния:
   - `{000...0}` (March)
   - `{111...1}` (March)
   - `{000...1}` (Walking Ones)
   - `{111...0}` (Walking Zeros)
   - И все промежуточные состояния
3. **Полный набор состояний** = гарантия обнаружения

---

## 💻 Функциональность приложения

### Пользовательский интерфейс

#### 1. **Панель управления неисправностями** 🔧

- **Выбор модели неисправности**: Dropdown с 4 типами + "Нет"
- **Настройка адреса**: Ввод начального адреса (0-255)
- **Настройка длины**: Количество слов с неисправностью (1-256)
- **Вероятность инверсии**: Для Bit-flip (0.0 - 1.0)
- **Кнопки**:
  - "Внедрить неисправность" — применение выбранной неисправности
  - "Сбросить память" — очистка памяти и удаление неисправностей
- **Информация**: Отображение текущей внедренной неисправности

#### 2. **Панель тестирования** 🧪

- **Выбор алгоритма**: Walking 1s, Walking 0s, March - simple
- **Описание алгоритма**: Динамическое описание выбранного алгоритма
- **Кнопка "Запустить тест"**: Запуск асинхронного тестирования
- **Прогресс-бар**: Отображение прогресса тестирования (0-100%)
- **Детали прогресса**:
  - Текущий адрес
  - Ожидаемое значение
  - Прочитанное значение

#### 3. **Панель статистики** 📊

- **Всего адресов**: 256
- **Протестировано**: Количество проверенных адресов
- **Найдено неисправностей**: Количество обнаруженных ошибок
- **Покрытие**: Процент протестированных адресов
- **Время теста**: Длительность выполнения теста
- **Текущая модель**: Тип внедренной неисправности
- **Текущий алгоритм**: Выбранный алгоритм тестирования

#### 4. **Таблица памяти** 📋

| Колонка | Описание |
|---------|----------|
| **Адрес** | Номер ячейки памяти (0-255) |
| **HEX** | Значение в шестнадцатеричном формате |
| **Двоичное** | Значение в двоичном формате |
| **Десятичное** | Значение в десятичном формате |
| **Статус** | Исправна / Неисправна / Не протестировано |
| **Тип неисправности** | Тип внедренной или обнаруженной неисправности |

**Цветовая индикация**:
- 🟢 **Зеленый**: Исправные ячейки
- 🔴 **Красный**: Обнаруженные неисправности
- 🟡 **Желтый**: Область с внедренной неисправностью (еще не протестирована)
- ⚪ **Серый**: Непротестированные адреса

**Функции навигации**:
- Поиск по адресу
- Переход к следующей неисправности (с циклическим поиском)

#### 5. **Журнал событий** 📝

- **Типы сообщений**:
  - ℹ️ INFO: Информационные сообщения
  - ⚠️ WARNING: Предупреждения
  - ❌ ERROR: Ошибки
  - ✅ SUCCESS: Успешные операции
- **Временные метки**: Для каждого события
- **Функции**:
  - Очистка журнала
  - Автоматическая прокрутка к новым сообщениям

#### 6. **Экспорт результатов** 💾

- **Формат**: CSV (совместим с Excel)
- **Содержимое**:
  - Адрес
  - Ожидаемое значение (HEX и Decimal)
  - Прочитанное значение (HEX и Decimal)
  - Статус (PASS/FAIL)
- **Кодировка**: UTF-8 с BOM для корректного отображения в Excel

### Основные возможности

| Возможность | Описание |
|-------------|----------|
| 🎯 **Моделирование неисправностей** | Внедрение 4 типов неисправностей с настраиваемыми параметрами |
| 🧪 **Тестирование алгоритмов** | Запуск 3 алгоритмов обнаружения неисправностей |
| 📊 **Визуализация результатов** | Цветовая индикация, таблица памяти, статистика |
| 🔍 **Навигация** | Поиск адресов, переход к неисправностям |
| 💾 **Экспорт данных** | Сохранение результатов в CSV |
| ⚡ **Асинхронность** | Многопоточное выполнение тестов без блокировки UI |
| 📈 **Метрики** | Статистика покрытия, времени выполнения, обнаруженных ошибок |

---

## 🔬 Технические детали реализации

### Технологический стек

| Компонент | Технология |
|-----------|-----------|
| **Язык программирования** | C++ |
| **Фреймворк UI** | Qt Framework |
| **Архитектура** | Многопоточность (QThread) |
| **Синхронизация** | QMutex для потокобезопасности |
| **Сигналы и слоты** | Qt Signal/Slot механизм |

### Архитектурные решения

#### 1. **Потокобезопасность**

```cpp
// MemoryModel использует QMutex для защиты данных
Word MemoryModel::read(size_t addr) {
    QMutexLocker locker(&_mutex);
    // ... безопасный доступ к данным
}
```

**Преимущества**:
- Безопасный доступ из разных потоков
- Предотвращение race conditions
- Корректная работа при асинхронном тестировании

#### 2. **Многопоточность**

```cpp
// TesterWorker создает отдельный поток для тестирования
TesterWorker::TesterWorker(MemoryModel* mem, QObject* parent)
    : QObject(parent), _thread(), _mem(mem) {
    this->moveToThread(&_thread);
    _thread.start();
}
```

**Преимущества**:
- UI остается отзывчивым во время тестирования
- Возможность отображения прогресса в реальном времени
- Не блокирует пользовательский интерфейс

#### 3. **Применение неисправностей**

**Ключевое решение**: Неисправности применяются только при чтении

```cpp
Word MemoryModel::read(size_t addr) {
    Word stored_value = _words[addr]; // Всегда читаем фактическое значение
    
    if (!_isAddrFaulty(addr)) 
        return stored_value;
    
    // Применяем неисправность только при чтении
    switch (_injected.model) {
        case FaultModel::StuckAt0: return 0u;
        // ...
    }
}

void MemoryModel::write(size_t addr, Word value) {
    // ЗАПИСЬ ВСЕГДА СОХРАНЯЕТ ТОЧНОЕ ЗНАЧЕНИЕ
    _words[addr] = value;
}
```

**Преимущества**:
- Реалистичное моделирование (неисправность влияет на чтение)
- Возможность тестирования алгоритмов записи
- Корректная работа тестов (запись всегда успешна)

#### 4. **Вероятностные неисправности**

```cpp
// Bit-flip использует вероятностное распределение
case FaultModel::BitFlip: {
    Word v = stored_value;
    std::bernoulli_distribution bitProb(_injected.flip_probability);
    for (int b = 0; b < 32; ++b) 
        if (bitProb(_rng)) 
            v ^= (1u << b);  // Инверсия бита с вероятностью
    return v;
}
```

**Особенности**:
- Использование `std::mt19937` для генерации случайных чисел
- Вероятностное применение на уровне адреса и бита
- Реалистичное моделирование случайных ошибок

### Структура данных

#### MemoryModel

```cpp
class MemoryModel {
    std::vector<Word> _words;        // Хранилище данных (256 слов)
    InjectedFault _injected;         // Текущая неисправность
    mutable QMutex _mutex;            // Синхронизация
    mutable std::mt19937 _rng;       // Генератор случайных чисел
};
```

#### TestResult

```cpp
struct TestResult {
    size_t addr;      // Адрес ячейки
    Word expected;    // Ожидаемое значение
    Word read;        // Прочитанное значение
    bool passed;      // Результат проверки
};
```

### Производительность

| Метрика | Значение |
|---------|----------|
| **Размер памяти** | 256 слов (32-битных) = 1 КБ |
| **Время теста** | ~1-3 секунды (зависит от алгоритма) |
| **Покрытие** | 100% адресов при полном тесте |
| **Потокобезопасность** | Гарантирована через QMutex |

---

## 📊 Результаты и метрики

### Метрики покрытия

#### 1. **Адресное покрытие**

```
Coverage_addr = (tested_addresses / total_addresses) × 100%
```

- **Всего адресов**: 256
- **При полном тесте**: 100% покрытие
- **Отображение**: В панели статистики

#### 2. **Битовое покрытие**

```
Coverage_bit = (unique_bit_patterns / 2^32) × 100%
```

- **Walking Ones/Zeros**: 100% покрытие всех битов
- **Каждый бит проверяется**: От bit 0 до bit 31
- **Все позиции**: Все 32 бита в каждом слове

#### 3. **Неисправностное покрытие**

```
Coverage_fault = (detected_faults / injected_faults) × 100%
```

- **Детерминированные неисправности**: 100% обнаружение
- **Вероятностные (Bit-flip)**: Зависит от вероятности и количества тестов

### Статистика тестирования

#### Отображаемые метрики

| Метрика | Описание |
|---------|----------|
| **Всего адресов** | Общее количество ячеек памяти (256) |
| **Протестировано** | Количество проверенных адресов |
| **Найдено неисправностей** | Количество обнаруженных ошибок |
| **Покрытие** | Процент протестированных адресов |
| **Время теста** | Длительность выполнения теста |
| **Текущая модель** | Тип внедренной неисправности |
| **Текущий алгоритм** | Выбранный алгоритм тестирования |

#### Примеры результатов

**Сценарий 1: Stuck-at-0 обнаружен**
```
Всего проверок: 256
Обнаружено неисправностей: 1
Покрытие: 100%
Время: 1.23 сек
Статус: ✅ Неисправность обнаружена
```

**Сценарий 2: Нет неисправностей**
```
Всего проверок: 256
Обнаружено неисправностей: 0
Покрытие: 100%
Время: 1.15 сек
Статус: ✅ Все проверки прошли успешно
```

### Эффективность алгоритмов

| Алгоритм | Время выполнения | Покрытие битов | Обнаружение Stuck-at | Обнаружение Bit-flip |
|----------|------------------|----------------|---------------------|---------------------|
| **Walking Ones** | ~1.5 сек | 100% | ✅ Stuck-at-0 | ✅ (вероятностно) |
| **Walking Zeros** | ~1.5 сек | 100% | ✅ Stuck-at-1 | ✅ (вероятностно) |
| **March Simple** | ~1.0 сек | 100% | ✅ Stuck-at-0/1 | ✅ (вероятностно) |

### Визуализация результатов

#### Цветовая индикация в таблице

- 🟢 **Зеленый фон**: Исправные ячейки (тест пройден)
- 🔴 **Красный фон**: Обнаруженные неисправности (тест не пройден)
- 🟡 **Желтый фон**: Область с внедренной неисправностью (еще не протестирована)
- ⚪ **Серый фон**: Непротестированные адреса

#### Статусы ячеек

- **"Исправна"**: Тест пройден, неисправностей не обнаружено
- **"Неисправна"**: Тест не пройден, обнаружена неисправность
- **"Не протестировано"**: Тест еще не выполнялся для этого адреса

---

## 🎓 Выводы и достижения

### Достигнутые цели

✅ **Создана виртуальная модель ОЗУ**
- Реализована модель памяти с поддержкой чтения/записи
- Имитировано поведение реальной памяти

✅ **Реализовано моделирование неисправностей**
- 4 типа неисправностей с настраиваемыми параметрами
- Реалистичное применение неисправностей (только при чтении)

✅ **Реализованы алгоритмы тестирования**
- 3 алгоритма обнаружения неисправностей
- 100% покрытие битов и адресов

✅ **Создан пользовательский интерфейс**
- Интуитивный GUI с визуализацией результатов
- Статистика и метрики в реальном времени

✅ **Реализована многопоточность**
- Асинхронное тестирование без блокировки UI
- Отображение прогресса в реальном времени

### Ключевые достижения

1. **Полное покрытие неисправностей**
   - Комбинация алгоритмов обеспечивает обнаружение всех типов неисправностей
   - Теоретически обоснованная гарантия обнаружения

2. **Реалистичное моделирование**
   - Неисправности применяются только при чтении (как в реальности)
   - Вероятностные неисправности с настраиваемыми параметрами

3. **Удобный интерфейс**
   - Визуальная индикация состояния памяти
   - Навигация по неисправностям
   - Экспорт результатов для анализа

4. **Производительность**
   - Быстрое выполнение тестов (~1-3 секунды)
   - Отзывчивый UI благодаря многопоточности

### Практическая ценность

| Область применения | Ценность |
|-------------------|---------|
| **Образование** | Изучение работы памяти и неисправностей без риска |
| **Разработка** | Тестирование алгоритмов обнаружения ошибок |
| **Исследования** | Экспериментирование с различными типами неисправностей |
| **Обучение** | Визуализация процесса тестирования памяти |

### Теоретическая значимость

- ✅ **Ортогональность тестов**: Каждый алгоритм проверяет различные свойства
- ✅ **Полнота покрытия**: 100% покрытие всех битов и адресов
- ✅ **Комплементарность**: Комбинация алгоритмов гарантирует обнаружение

### Будущие улучшения

🔮 **Возможные расширения**:
- Дополнительные алгоритмы тестирования (March C-, Butterfly, и др.)
- Более сложные модели неисправностей
- Графическая визуализация результатов
- Сравнение эффективности алгоритмов
- Автоматический выбор оптимального алгоритма

---

## 📚 Заключение

### Резюме проекта

**Цифровой двойник ОЗУ — Тестбенч** — это полнофункциональное приложение для моделирования неисправностей оперативной памяти и тестирования алгоритмов их обнаружения.

### Основные преимущества

- 🛡️ **Безопасность**: Моделирование без риска для оборудования
- 🎯 **Точность**: Реалистичное моделирование поведения памяти
- 📊 **Метрики**: Детальная статистика и покрытие
- 🎨 **Удобство**: Интуитивный интерфейс с визуализацией
- ⚡ **Производительность**: Быстрое выполнение и отзывчивый UI

### Благодарности

Благодарим **Савчица Андрея Геннадьевича** и **Татура Михаила Михайловича** за руководство и поддержку в разработке проекта.

---

**Версия**: 1.0  
**Дата**: 2024  
**Команда**: Волосевич В.А., Прокопович Д.В., Шкелёнок А.В., Харькова М.С.

---

*Презентация подготовлена на основе анализа кода проекта и теоретической базы*

